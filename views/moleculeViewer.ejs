<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.3/css/dataTables.bootstrap5.min.css">
    <title>ChemAR</title>

    <style>
        body {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            color: white;
            background: #202020;
        }

        .content {
            border: 2px solid #333;
            padding: 20px;
            margin-bottom: 20px;
            border-radius: 4px;
        }

        .content-display {
            border: 2px solid #333;
            padding: 20px;
            margin-bottom: 20px;
        }

        .content-jsmol {

            border: 2px solid #333;
            padding: 20px;
            margin-bottom: 20px;

            display: flex;
            justify-content: center;
            align-items: center;
        }

        #control-panel {
            display: flex;
            justify-content: center;
            padding: 20px;
            align-items: center;
            border: 2px solid #333;
            margin-bottom: 20px;
            border-radius: 4px;
        }

        input[type=button] {
            background-color: rgb(251, 251, 251);
            border-radius: 4px;
            border: 2px solid #333;
        }
    </style>

</head>

<body>
    <script src="https://www.gstatic.com/firebasejs/9.6.8/firebase-app-compat.js"></script>
    <script src="https://www.gstatic.com/firebasejs/9.6.8/firebase-database-compat.js"></script>
    <script src="/jmol-16.1.41/jsmol/JSmol.min.js"></script>
    <script src="/jmol-16.1.41/jsmol/js/Jmol2.js"></script>

    <div id="backButton"> 
        <a href="/molecules">
            <input type="button" value="Back to Molecules">        
        </a>
    </div>
    
    <div id="jmolApplet"></div>

    <script>
        const currentURL = window.location.href;
        const urlParts = currentURL.split('/');
        const chemSpiderId = urlParts[urlParts.length - 1];

        console.log("ChemSpider ID:", chemSpiderId);

        Jmol.Info.j2sPath = '/jmol-16.1.41/jsmol/j2s';
        Jmol.Info.serverURL = '/jmol-16.1.41/jsmol/php/jsmol.php';
        jmolInitialize('/jmol-16.1.41/jsmol', true);

        async function loadMolFile() {
            try {

                var cid = 34668;
                var molFile = await fetch(`/molfiles/${cid}.mol`);
                var molFileData = await molFile.text();

                console.log(molFileData);
                molData = molFileData.split("END")[0].trim();
                const modifiedMolData = '|' + molData.replace(/\n/g, '|').replace(/^\|+|\|+$/g, '') + " END|";
                console.log(modifiedMolData)

                // jmolSetAppletColor('0x202020');
                // jmolApplet(['600', '600'], `set antialiasdisplay;load inline "${modifiedMolData}"$$$$;spin on;background HOVER[NONE];`, '0');

                return Promise.resolve(modifiedMolData);
            } catch (error) {
                console.error('Error loading Mol file:', error);
                alert('Error loading Mol file');
                return Promise.reject(new Error("Something went wrong"));
            }
        }

        function loadJmol(data) {   
            jmolSetAppletColor('0x202020');
            jmolApplet(['600', '600'], `set antialiasdisplay;load data \"mydata\""${data}"$$$$||end\"mydata\"";spin on;background HOVER[NONE];`, '0');
            jmolButton("spin @ {!_spinning}", "Toggle Spin");
        }

        loadMolFile()
            .then(data => {
                console.log(data);
                loadJmol(data);
            })
            .catch(error => {
                console.log(error);
            });

    // jmolSetAppletColor('0x202020');
    // jmolApplet(['600', '600'], `set antialiasdisplay;load inline"$$$$;spin on;background HOVER[NONE];`, '0');
    // jmolButton("spin @ {!_spinning}", "Toggle Spin");
</script>

    <div id="control-panel">
        <!-- bond size dropdown -->
        <label for="bond-size-dropdown">Bond Size</label>
        <select id="bond-size-dropdown" onchange="changeBondSize(this.value)">
            <option value="0.1">0.1</option>
            <option value="0.2">0.2</option>
            <option value="0.3">0.3</option>
            <option value="0.4">0.4</option>
            <option value="0.5">0.5</option>
        </select>
    </div>

    <div id="control-panel2">
        <!--spin switch-->
        <input type="button" value="Open Console" id="console-open" onclick="jmolScript('console;')">
        <input type="button" value="Get obj file" id="get-obj" onclick='jmolScript("write " + chemSpiderId + ".obj;")'>
    </div>
</body>

</html>
